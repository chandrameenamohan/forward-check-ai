# AGENTS.md — ForwardCheck-AI Operational Guide

## Project Info

- **Name:** ForwardCheck-AI
- **Language:** TypeScript (strict ESM, NodeNext)
- **Runtime:** Node.js 20+
- **Root:** `/Users/ralph/Projects/forward-check-ai`
- **Package manager:** npm
- **Test framework:** Vitest
- **Branch:** develop

## Build & Run

```bash
# Install dependencies
npm install

# Type check
npx tsc --noEmit

# Run tests
npx vitest run

# Run dev server
npx tsx src/index.ts

# Run bot only
npx tsx src/bot.ts
```

## Validation

Run these after implementing to get immediate feedback:

- **Tests:** `npx vitest run`
- **Typecheck:** `npx tsc --noEmit`

## Architecture Overview

### Pipeline Flow

```
Telegram Message → Classifier (Haiku) → Claim Strategist (Opus 4.6)
  → 2-3 Parallel Investigators (Sonnet 4.5) → Devil's Advocate (Opus 4.6)
  → Judge (Opus 4.6) → VerdictFormatter (code) → Telegram Reply + Web Page
```

### Key Modules

| Module | Location | Responsibility |
|--------|----------|---------------|
| Bot | `src/bot/` | Telegram bot, message handling, status updates |
| Server | `src/server/` | Express server, health endpoint, verdict pages |
| Database | `src/db/` | SQLite connection, investigation repository |
| Schemas | `src/schemas/` | Zod schemas for all agent I/O |
| Services | `src/services/` | Claude client wrapper, claim cache |
| Agents | `src/agents/` | Agent implementations (classifier, strategist, investigators, DA, judge) |
| Tools | `src/tools/` | Search tools (Brave, Google Fact Check) |
| Orchestrator | `src/orchestrator/` | Pipeline orchestration, agent runner, tool-use loop |
| Formatter | `src/formatter/` | Verdict formatting for Telegram + web |
| Config | `src/config/` | Environment config, logger setup |

### Data Flow

1. User forwards message to Telegram bot
2. Bot saves to SQLite, sends "Investigating..." status
3. Orchestrator runs pipeline:
   a. Classifier (Haiku) → ClassifierResult
   b. Claim Strategist (Opus 4.6) → SearchStrategy
   c. 2-3 Investigators (Sonnet 4.5) in parallel → AgentReport[]
   d. Devil's Advocate (Opus 4.6) → ChallengeReport
   e. Judge (Opus 4.6 + brave_web_search) → FinalVerdict
4. VerdictFormatter produces Telegram HTML + saves to DB
5. Bot sends verdict with "View Full Analysis" link
6. Web server renders `/v/:id` verdict page

## Coding Standards

- **Strict TypeScript** — `strict: true` in tsconfig, no `any`
- **ESM modules** — `"type": "module"` in package.json, `.js` extensions in imports
- **Max 400 lines per file** — extract if larger
- **No `console.log`** — use Pino logger from `src/config/logger.ts`
- **Zod for validation** — all agent I/O validated through Zod schemas
- **Descriptive test names** — `"should return likely-false when confidence < 29%"`
- **Test isolation** — each test independent, no shared mutable state
- **Parameterized SQL** — never interpolate user input

## Conventions

- **File naming:** kebab-case (`agent-runner.ts`, `classifier-agent.ts`)
- **Class naming:** PascalCase (`ClassifierAgent`, `VerdictFormatter`)
- **Test files:** mirror source structure in `tests/` with `.test.ts` suffix
- **Barrel exports:** `index.ts` in each module directory
- **Environment variables:** loaded via `src/config/env.ts`, validated at startup

## Gotchas

- **Zod v4 installed (4.3.6):** The `z` import and API (`z.object`, `z.string`, `z.enum`, etc.) are compatible with the spec's schema definitions. Import as `import { z } from "zod"`.
- **Express v5 installed (5.2.1):** Express 5 is the latest major. Router API is mostly the same as v4 but `req.params` returns `Record<string, string | undefined>` and error handling has minor differences.
- **Zod v4 `.default()` + `.transform()` ordering:** In Zod v4, `.default()` placed AFTER `.transform().pipe()` returns the raw default value without running the transform. Place `.default()` BEFORE `.transform()` so the default feeds through the pipeline. Example: `z.string().default("3000").transform(val => parseInt(val, 10)).pipe(z.number())`.
- **dotenv v17 logs injection messages to stdout:** `dotenv@17.2.4` prints "[dotenv@17.2.4] injecting env (N) from .env" on every `config()` call. This is cosmetic noise in tests but harmless.
- **SQLite `datetime('now')` has second-level granularity:** Rapid sequential inserts get identical `created_at` values, making `ORDER BY created_at DESC` non-deterministic. Use `ORDER BY rowid DESC` for reliable insertion-order sorting.
- **Vitest 4 `it()` options API:** Vitest 4 removed the `it(name, fn, { timeout })` 3-arg signature. Use `it(name, { timeout }, fn)` or `it.skipIf(cond)(name, { timeout }, fn)` instead. The old pattern was deprecated in Vitest 3.
- **Anthropic SDK adaptive thinking effort:** Effort levels (`low`, `medium`, `high`, `max`) are on `output_config.effort` in `MessageCreateParamsBase`, NOT on `ThinkingConfigAdaptive`. `ThinkingConfigAdaptive` only has `{ type: "adaptive" }`. For agents that need different effort levels, set `output_config: { effort: "high" }` alongside `thinking: { type: "adaptive" }`.
- **Tool-use for structured output pattern:** For agents with complex nested output schemas (like Strategist), use a `submit_*` tool instead of instructing JSON text output. Tool-use inputs are already structured objects — no JSON parsing or markdown-fence stripping needed. The model calls the tool with structured data, and you validate with Zod directly. More reliable than text-based JSON for deeply nested schemas.
- **Grammy bot testing pattern:** To test handlers without a real Telegram connection: 1) Set `bot.botInfo` to a fake `UserFromGetMe` object after creating the bot. 2) Use `bot.api.config.use((prev, method, payload) => ...)` to intercept outgoing API calls (e.g., `sendMessage`). 3) Call `bot.handleUpdate(update)` with a manually constructed `Update` object. No external test libraries needed.
- **Agent runner `outputConfig` for effort levels:** `runAgent` now accepts `outputConfig?: { effort: "low" | "medium" | "high" | "max" }` which is spread as `output_config` into `client.createMessage()`. Use this when an agent needs adaptive thinking effort levels through the multi-turn loop (e.g., Judge with effort "max"). Previously, agents needing effort levels had to call `client.createMessage()` directly (like DA), but the Judge needs both multi-turn tool use AND effort control.
- **Agent runner loop continues after submit_* tool calls:** `runAgent` processes `submit_report`/`submit_*` tools like any other tool call — executes the callback, pushes tool result to messages, and loops again (since `stop_reason === "tool_use"`). The loop only breaks on `end_turn` or when maxTurns is exhausted. In tests, always add an `END_TURN_RESPONSE` mock after the `submit_*` mock, or rely on maxTurns to terminate. The real API will naturally end after receiving the tool result acknowledgment.
- **EJS include for CSS partials:** Use `<%- include('_partial-name') %>` (unescaped output `<%-`) for including EJS partials that contain HTML/CSS. The partial file name starts with underscore by convention (e.g., `_verdict-styles.ejs`). The partial has access to all template locals from the parent template (e.g., `verdict.category` works inside the included file). Note: EJS `include` paths are relative to the template's directory, not the project root.
- **Opus 4.6 may serialize nested tool_use fields as JSON strings:** When using `submit_*` tools with deeply nested schemas (like Strategist's `investigatorGuidance`), Opus sometimes returns a nested object as a JSON string instead of a parsed object. The Strategist agent now includes a recovery step that JSON-parses any string fields that look like JSON objects before Zod validation. If this pattern recurs in other agents, apply the same fix.
- **Pipeline error propagation semantics:** The pipeline only gracefully handles investigator failures (via `Promise.allSettled`). Classifier, Strategist, DA, and Judge failures propagate as thrown errors — there is no skip-and-continue logic for these agents. Resilience tests should verify this propagation behavior rather than assume graceful degradation for all agents. DB persistence is partial on failure: results are saved up to the point of failure (e.g., classifier result saved before strategist throws).
- **Confidence gate vs. Judge confidence semantics mismatch:** The confidence gates interpret the score as "truth likelihood" (85-100 = likely-true, 0-29 = likely-false), but the Judge produces confidence as "verdict confidence" (97 = 97% sure my verdict is correct). When the Judge says `likely-false` with 97% confidence, the gate overrides it to `likely-true`. This is a known design tension — future task should align the Judge's prompt to produce confidence as truth-likelihood, or invert the score for negative verdicts before gate enforcement.
- **E2E pipeline takes ~190s with real API calls:** The full pipeline with 6 agents (Haiku + Opus + 3×Sonnet + Opus + Opus) costs ~$0.50-0.60 and takes ~3 minutes. The Judge with effort "max" is the bottleneck (~60-90s for 3 turns). Integration test timeout must be ≥300s.
- **Investigator failures are common with real search APIs:** With real Brave/Google Fact Check search tools, 1-2 of 3 investigators may fail (e.g., text parse fallback fails if model doesn't call submit_report and text output doesn't match AgentReport schema). Pipeline continues gracefully with ≥1 report. Seed demo consistently gets 2-3 successful investigators out of 3.
- **Seed script env bootstrapping:** The seed script sets a dummy `TELEGRAM_BOT_TOKEN` env var when not present (required by `loadEnv()` Zod validation) since the bot isn't used during seeding. This avoids requiring a real bot token just to seed demo data.
- **Stale compiled `.js` files in `src/` break tests:** If `tsc` is run with the default `outDir` pointing into `src/` (or no outDir), compiled `.js` files land next to `.ts` sources. Vitest then resolves `.js` imports to the compiled CommonJS files instead of the `.ts` sources, causing `"exports is not defined in ES module scope"` and `"better_sqlite3_1.default is not a constructor"` errors. Fix: delete all `src/**/*.js` files. The `.gitignore` should exclude `src/**/*.js` to prevent recurrence.
- **Landing page EJS partial extraction for 400-line limit:** When `landing.ejs` exceeds 400 lines, extract sections into `_<section-name>.ejs` partials (e.g., `_verdict-preview.ejs`, `_opus-showcase.ejs`). EJS `<%- include('_partial-name') %>` renders the partial inline — tests that check for CSS classes or content in the rendered HTML work identically whether the section is inline or in a partial. No route changes needed.

## Conventions

- **Env config testing pattern:** `loadEnv()` accepts an optional `env` record for testing. Pass env vars directly instead of manipulating `process.env` to avoid interference from `.env` file loading.
- **Investigator agent pattern:** All 3 investigators follow the same shape: `run*(claim, searchStrategy, client, toolRegistry)` → `{ report: AgentReport, costUsd }`. They use `runAgent` with maxTurns=4, inject search strategy guidance into the system prompt, use `submit_report` tool for structured AgentReport output, and fall back to text JSON parsing. Only the system prompt and tool set differ between investigators.
- **Shared test fixtures:** `tests/fixtures/` provides factory functions (`makeClassifierResult`, `makeSearchStrategy`, `makeAgentReport`, `makeChallengeReport`, `makeFinalVerdict`, `makeInvestigation`) and canned search results (`makeCannedBraveResults`, `makeCannedFactCheckResults`). Import from `tests/fixtures/index.js`. Each factory accepts `Partial<T>` overrides spread at top level; `makeSearchStrategy` does deep merge on nested objects (`claimCharacteristics`, `investigatorGuidance`, `falsificationCriteria`).

## Decisions Log

- **Task 0.1:** Used `noUncheckedIndexedAccess: true` in tsconfig for extra safety on array/object indexing. Used `isolatedModules: true` for compatibility with transpilers. Vitest config kept minimal — no globals, file pattern `tests/**/*.test.ts`.
- **Task 0.2:** Installed all production deps. Zod resolved to v4 (4.3.6) — API is backward-compatible with spec schemas. Express resolved to v5 (5.2.1). All 9 production packages + 3 dev type packages installed and importable.
- **Task 0.3:** Env config uses Zod for validation with `loadEnv(env?)` pattern. `dotenv.config()` called inside `loadEnv()` only when no custom env is provided. Added `dotenv` as production dependency. `.env.example` already existed from prior setup.
- **Task 0.4:** Logger uses `createLogger(options?)` factory pattern with `LoggerOptions` interface (`level`, `pretty`). Pretty printing via pino `transport` config (not the deprecated `prettyPrint` option). Defaults to `info` level and JSON output. Consumers will call `createLogger({ level: config.LOG_LEVEL, pretty: config.NODE_ENV === "development" })`.
- **Task 1.1:** Database connection uses `createDatabase(dbPath)` factory function. Enables WAL mode and foreign keys via pragmas. Auto-creates parent directories with `mkdirSync(dirname(dbPath), { recursive: true })`. Tests use `os.tmpdir()` with random UUIDs for isolation, and clean up `-wal`/`-shm` files in afterEach.
- **Task 1.2:** Migrations use `runMigrations(db)` with `CREATE TABLE IF NOT EXISTS` for idempotency. All JSON columns (classifier_result, search_strategy, agent_reports, challenge_report, final_verdict) use the `JSON` column type — stored as TEXT in SQLite but semantically typed. Default values: `status='pending'`, `created_at=datetime('now')`, `total_cost_usd=0`.
- **Task 1.3:** `InvestigationRepository` class wraps better-sqlite3 with typed CRUD methods. JSON columns serialized with `JSON.stringify()` on write, `JSON.parse()` on read. Uses `nanoid()` for ID generation (21-char default). `getRecent()` orders by `rowid DESC` instead of `created_at DESC` because `datetime('now')` has second-level granularity — multiple inserts within the same second get identical timestamps.
- **Task 3.1:** `ClaudeClient` class wraps Anthropic SDK with `createMessage()` and `estimateCost()`. Pricing: Haiku $1/$5, Sonnet $3/$15, Opus $5/$25 per MTok (input/output). Thinking tokens billed as output tokens. Exposes `_client` getter for test mocking — assign `client._client.messages.create` directly. QA test uses `it.skipIf(!apiKey)` to auto-skip when no API key is set. `MODELS` constant exported with all 3 model IDs.
- **Task 3.2:** `runAgent(config)` function implements the core agentic tool-use loop. Uses `Promise.race` for timeout enforcement (not AbortController signal — mocks don't respect it). Internal `runAgentLoop()` handles the multi-turn loop: calls `client.createMessage()`, checks for `tool_use` blocks, executes tools via `onToolCall` callback, pushes assistant content + tool results to messages array, loops until `stop_reason !== "tool_use"` or maxTurns reached. Tool errors are caught and returned as `is_error: true` tool_result blocks (never thrown). Thinking config passed through to API. Token usage and cost accumulated across turns. `ContentBlockParam` requires cast from `response.content` due to SDK type narrowing.
- **Task 4.1:** `braveWebSearch(query, count, apiKey)` function wraps the Brave Search API. Returns `{ results: BraveSearchResult[] }` with `title`, `url`, `description`, `age` fields. API key passed as parameter (not read from env directly) so tool registry can inject it. Graceful error handling: returns `{ results: [] }` on HTTP errors or network failures. `braveSearchToolDefinition` exported as a Claude `Tool` type for agent tool configs. Tests mock global `fetch` via `vi.stubGlobal("fetch", mockFn)`.
- **Task 4.2:** `googleFactCheckSearch(query, apiKey)` function wraps the Google Fact Check Tools API (`v1alpha1/claims:search`). Returns `{ claims: FactCheckClaim[] }` with `text`, `claimant`, and `claimReviewMarkup` (containing `url`, `title`, `publisher`, `rating`). Follows same pattern as Brave Search: API key as parameter, graceful error handling returning `{ claims: [] }`. Claims without `claimReview` arrays are filtered out. When a claim has multiple reviews, uses the first one. `googleFactCheckToolDefinition` exported for Claude tool configs.
- **Task 4.3:** `ToolRegistry` class maps tool names to handlers and Claude tool definitions. Methods: `register(name, handler, definition)`, `execute(name, input)` → `Promise<string>`, `getToolDefinitions()` → `Tool[]`. Unknown tools return error strings (not thrown). Handler execution errors are caught and returned as error strings. Handlers can be sync or async — both are awaited. The agent runner's `onToolCall` callback delegates to `registry.execute()`. Tool definitions are stored alongside handlers so `getToolDefinitions()` returns them in registration order.
- **Task 5.1:** `createApp()` factory function creates and configures Express 5 app. Sets up JSON body parsing, EJS view engine (views in `src/server/views/`), `/health` endpoint returning `{ status, timestamp, uptime }`, 404 handler, and error handling middleware. Does NOT call `app.listen()` — server startup belongs in the entry point (`src/index.ts`). Tests use `app.listen(0)` on a random port with native `fetch` (no supertest needed). Express 5 error handler requires explicit 4-param signature `(err, req, res, next)` to be recognized as error middleware. `__dirname` derived from `import.meta.url` for ESM compatibility.
- **Task 5.2:** `createApp(repo?)` now accepts an optional `InvestigationRepository`. When provided, mounts `createInvestigateRouter(repo)` which provides `POST /api/investigate` (validates message, creates investigation, returns `{ id, status }` with 201) and `GET /api/investigation/:id` (returns investigation or 404). Routes in `src/server/routes/investigate.ts` use Express `Router`. Input validation is manual (checks non-empty string) since this is an internal API — Zod validation happens at the agent layer. Tests create isolated SQLite DBs in `os.tmpdir()` per test.
- **Task 6.1:** `createBot(token)` factory function creates and configures Grammy Bot instance. Does NOT register message handlers — `createMessageHandler()` (Task 14.1) wires the pipeline. Does NOT call `bot.start()` — that belongs in the entry point. Tests use `bot.handleUpdate(update)` with mock Update objects and intercept `api.sendMessage` via `bot.api.config.use()` transformer. `bot.botInfo` must be set before calling `handleUpdate` to skip the `getMe` API call.
- **Task 6.2:** `StatusUpdater` class takes `Api<RawApi>` and `chatId` in constructor. `sendInitial()` sends "Investigating your claim..." via `api.sendMessage()` and saves the returned `message_id`. `update(stage)` edits that message via `api.editMessageText()` with stage-appropriate text from `PIPELINE_STAGES` constant. `sendVerdict(html)` sends a new message with `parse_mode: "HTML"`. All methods catch errors gracefully (log and continue). Exported `PipelineStage` type and `PIPELINE_STAGES` record for 5 stages: planning, searching, analyzing, challenging, judging. Tests intercept Grammy API calls via `bot.api.config.use()` transformer — return fake `sendMessage` results with `message_id` to enable subsequent `editMessageText` assertions.
- **Task 7.1:** `runClassifier(message, client)` function implements the Classifier agent using Haiku. Returns `ClassifierOutput` with `result: ClassifierResult` and `costUsd`. 1 turn, no tools, no thinking config. System prompt instructs JSON-only output with 5 categories (factual_claim, opinion, scam, greeting, other), domain detection, compound claim detection, and urgency assessment. Parses response text as JSON (strips markdown code fences if present), validates with Zod `ClassifierResultSchema`. Retries once on parse/validation failure, accumulating cost across attempts. Throws after retry exhaustion. Tests mock `client._client.messages.create` directly (same pattern as agent-runner tests). QA test validates real API classifies "Modi Rs 5000" as factual_claim.
- **Task 7.2:** `handleNonFactual(result: ClassifierResult)` returns `{ text: string, shouldInvestigate: false }` for non-factual categories. Pure function with no dependencies — maps category to static response text. Covers greeting (explains bot purpose), opinion (redirects to factual claims), scam (warning + safety tips), other (asks for a specific claim). Used by the pipeline to short-circuit before investigation.
- **Task 8.1:** `runStrategist(claim, classifierResult, client)` implements the Claim Strategist agent using Opus 4.6 with adaptive thinking (effort: "medium"). Returns `StrategistOutput` with `strategy: SearchStrategy` and `costUsd`. Uses `submit_strategy` tool for structured output instead of JSON text parsing — more reliable for complex nested schemas. 1 turn, no retry needed since tool_use output is structurally sound. Thinking excerpt extracted from first thinking block (first 500 chars) and injected into the strategy output, overriding whatever the model put in `thinkingExcerpt`. `output_config: { effort: "medium" }` is accepted by SDK types without `@ts-expect-error`. QA test uses a health claim ("WHO green tea cures cancer") to validate SearchStrategy schema end-to-end.
- **Task 9.1:** `runSourceVerification(claim, searchStrategy, client, toolRegistry)` implements the Source Verification investigator using Sonnet 4.5 with `runAgent` multi-turn loop. 4 turns max, tools: brave_web_search + google_fact_check_search + submit_report. System prompt dynamically injects `searchStrategy.investigatorGuidance.sourceVerification` (targetQueries, prioritySources, lookFor). Uses `submit_report` tool for structured output — same pattern as Strategist. Falls back to text-based JSON parsing if model doesn't call submit_report. `onToolCall` callback delegates search tools to `toolRegistry.execute()` and returns acknowledgment for submit_report. Returns `SourceVerificationOutput` with `report: AgentReport` and `costUsd`. Tests require END_TURN_RESPONSE after submit_report mock because `runAgent` loop continues after tool_use stop_reason — the loop only breaks on end_turn or maxTurns.
- **Task 9.2:** `runDomainExpertise(claim, domain, searchStrategy, client, toolRegistry)` implements the Domain Expertise investigator using Sonnet 4.5. Takes an extra `domain` parameter (from ClassifierResult) to select a domain-specific system prompt. `DOMAIN_FRAMING` record maps all 6 domains (public_health, geopolitics, economics, science, technology, general) to expert persona descriptions. Only uses brave_web_search (not google_fact_check_search) — filters tool definitions from registry by name. Same `submit_report` tool + fallback text parsing pattern as Source Verification. Returns `DomainExpertiseOutput` with `report: AgentReport` and `costUsd`.
- **Task 9.3:** `runPatternMatching(claim, searchStrategy, client, toolRegistry)` implements the Pattern Matching investigator using Sonnet 4.5. Same function signature as Source Verification (no extra domain param). Uses both brave_web_search + google_fact_check_search (like Source Verification). System prompt focuses on searching fact-checker databases (Snopes, PolitiFact, AltNews, BoomLive) and identifying misinformation patterns (zombie claims, chain messages, manipulated media). Guided by `searchStrategy.investigatorGuidance.patternMatching`. Returns `PatternMatchingOutput` with `report: AgentReport` and `costUsd`. All 3 investigators are now complete.
- **Task 10.1:** `runDevilsAdvocate(claim, agentReports, falsificationCriteria, client, effortLevel?)` implements the Devil's Advocate agent using Opus 4.6 with adaptive thinking. Default effort: "high", escalated to "max" when investigators disagree (passed by pipeline). Returns `DevilsAdvocateOutput` with `report: ChallengeReport` and `costUsd`. Same `submit_*` tool pattern as Strategist — uses `submit_challenge` tool for structured ChallengeReport output. Thinking excerpt extracted from first thinking block (first 500 chars), overrides model's thinkingExcerpt field. System prompt instructs adversarial review of all investigator reports against falsification criteria, requiring explicit SUCCEEDED/FAILED determination. `formatAgentReports()` helper renders AgentReport[] into readable text for the user message. No retry logic needed — tool_use output is structurally sound. Exported `EffortLevel` type for pipeline integration.
- **Task 11.1:** `runJudge(claim, agentReports, challengeReport, searchStrategy, client, toolRegistry)` implements the Judge agent using Opus 4.6 with adaptive thinking (effort: "max"). Uses `runAgent` multi-turn loop (3 turns max) with brave_web_search + submit_verdict tools. 4-phase system prompt: Strategize (review falsification criteria) → Synthesize (resolve investigator contradictions) → Evaluate (consider DA challenges, optionally verify via search) → Verdict (call submit_verdict). Returns `JudgeOutput` with `verdict: FinalVerdict` and `costUsd`. Thinking summary extracted from first thinking block (first 500 chars), overrides model's thinkingSummary field. Includes `formatChallengeReport()` helper to render ChallengeReport for the user message. `outputConfig` added to `AgentConfig` interface in agent-runner to support effort level passthrough. FinalVerdict includes confidence decomposition (4 sub-scores), manipulation techniques, DA outcome, and sources. All 6 pipeline agents are now complete.
- **Task 12.1:** `enforceConfidenceGates(verdict: FinalVerdict)` → `FinalVerdict` pure function that mechanically overrides the verdict category if the confidence score doesn't match the gate range. Gates: likely-true (85-100), partially-true (60-84), unverified (30-59), likely-false (0-29). Satire and opinion categories bypass gates entirely. Returns a new object (no mutation). Logs a warning via Pino when an override occurs. Located in `src/formatter/confidence-gates.ts` — first module in the new `src/formatter/` directory.
- **Task 12.2:** `formatTelegramVerdict(verdict: FinalVerdict)` → `string` pure function that converts a FinalVerdict into Telegram HTML. Uses `CATEGORY_DISPLAY` record mapping all 6 categories to emoji + uppercase label. Includes: verdict badge line (emoji + category + confidence% + optional nuanceTag), deep reasoning indicator when activated, summary text, top 2 manipulation techniques sorted by severity. HTML escapes user-facing text via `escapeHtml()`. Truncates to 3900 chars to stay within Telegram's ~4000 char limit. Uses only Telegram-safe HTML tags (`<b>`, `<i>`). Located in `src/formatter/telegram-formatter.ts`.
- **Task 13.1:** `InvestigationPipeline` class in `src/orchestrator/pipeline.ts` orchestrates the full 6-agent pipeline. Constructor takes `ClaudeClient`, `ToolRegistry`, `InvestigationRepository`. Method `investigate(message, options?)` → `InvestigateResult` with `{ verdict, investigationId, nonFactualResponse?, totalCostUsd, durationMs }`. Pipeline flow: Classify → short-circuit if non-factual → Strategist → 3 Investigators via `Promise.allSettled` → disagreement detection (spread > 30 → escalate DA effort to "max") → DA → Judge → confidence gates → DB save. Gracefully handles partial investigator failures (continues with ≥1 report, throws if all 3 fail). Status updates emitted via `onStatusUpdate` callback wrapped in try/catch so Telegram errors don't crash the pipeline. `deepReasoningActivated` flag injected into verdict before confidence gates. Falsification criteria for DA flattened from `whatWouldProveTrue` + `whatWouldProveFalse`. DB persistence at every step: create → updateClassifierResult → updateSearchStrategy → updateAgentReports → updateChallengeReport → updateFinalVerdict. Non-factual messages get status `completed_non_factual`.
- **Task 13.2:** E2E integration test in `tests/integration/pipeline-e2e.test.ts`. Uses real Anthropic API with mock search tools (canned Brave + Google Fact Check results for a known false claim: "PM Modi Rs 5000 direct transfer"). Test validates full pipeline: Classifier → Strategist → 3 Investigators → DA → Judge → confidence gates → DB. All Zod schemas validated. Uses `it.skipIf(!apiKey)` to auto-skip when no API key. 300s test timeout. Fixed Strategist JSON-string recovery bug discovered during E2E testing. Also discovered confidence gate semantic mismatch (documented in Gotchas). Pipeline cost: ~$0.55, duration: ~190s.
- **Task 14.1:** `createMessageHandler(bot, pipeline, baseUrl)` wires Grammy bot to `InvestigationPipeline`. Registers a `message:text` handler that: extracts text from forwarded/direct messages, creates `StatusUpdater` for progress updates, calls `pipeline.investigate()` with `onStatusUpdate` callback, sends formatted verdict via `formatTelegramVerdict()` + Grammy `InlineKeyboard.url()` for "View Full Analysis" link to `{baseUrl}/v/{id}`, handles non-factual short-circuit (sends plain text, no keyboard), and catches pipeline errors with user-friendly error message. `createBot()` in `bot.ts` was simplified to only create the bot + error handler — no longer registers placeholder `message:text` handler (that's now `createMessageHandler`'s job). Bot tests updated accordingly.
- **Task 15.1:** `createVerdictRouter(repo)` creates Express Router for `GET /v/:id` verdict page route. Three EJS templates: `verdict.ejs` (completed investigation with full data), `verdict-pending.ejs` (in-progress with auto-refresh), `verdict-not-found.ejs` (404). Route logic: if not found → 404 template; if pending/investigating → pending template; if completed → verdict template with parsed JSON columns (final_verdict, challenge_report, search_strategy, agent_reports) passed as template locals. Express 5 `req.params` returns `string | string[]` — handled with `Array.isArray()` guard. Wired into `createApp(repo)` alongside existing investigate routes.
- **Task 15.2:** Verdict page hero section built with professional dark theme design. Uses Bootstrap 5 CDN + custom CSS (no Bootstrap defaults). Font stack: DM Mono (monospace accents), Instrument Serif (display/summary), Satoshi (body). Color-coded verdict badge with CSS custom property `--fc-verdict-color` set dynamically via EJS ternary chain for all 6 categories. SVG confidence ring with animated `stroke-dasharray` (circumference 408 = 2 * pi * 65). Confidence decomposition as 4 horizontal bars with CSS animation. Deep Reasoning Mode indicator conditionally rendered. Noise texture overlay via inline SVG data URI. Template test in `tests/unit/server/views/verdict-render.test.ts` renders EJS directly with `ejs.render()` (no Express server needed for template-only tests). Remaining sections (manipulation, reasoning, agents, sources) present as styled placeholders for Tasks 15.3/15.4.
- **Task 15.3:** Added Manipulation Techniques cards (with severity bars using gradient fill), AI Reasoning section (combining DA + Judge thinking excerpts), and Falsification Criteria ("What Would Change This Verdict") section. DA outcome displayed as color-coded badge (green for failed counter-argument, red for succeeded, amber for partial). CSS extracted from `verdict.ejs` into `_verdict-styles.ejs` partial via `<%- include('_verdict-styles') %>` to keep the main template under 400 lines. The styles partial is 571 lines (pure CSS, no logic) — acceptable for a stylesheet. Tests in `tests/unit/server/views/verdict-sections.test.ts` verify all new sections render correctly with sample data, and degrade gracefully when optional data (challengeReport, falsificationCriteria, manipulationTechniques) is absent.
- **Task 15.4:** Replaced placeholder agent reports with Bootstrap 5 accordion (collapsible sections). Each agent report card shows: role label + confidence badge in the header, summary + overall assessment in the body, and nested finding cards with assessment badges (color-coded: green/supported, red/contradicted, gray/insufficient_evidence, amber/mixed), source links with credibility tags, and relevant snippet blockquotes. Sources section already existed with links and relevance tags. Original claim and pipeline metadata (duration, cost, ID) were already present from Task 15.2. Added Deep Reasoning indicator to the footer. Guard conditions `agentReports.length > 0` prevent empty section rendering. Styles partial grew to 717 lines (still pure CSS). Tests in `tests/unit/server/views/verdict-details.test.ts` verify accordion structure, agent report content, finding sources, footer metadata, and graceful degradation with empty/null data.
- **Task 16.1:** `src/index.ts` is the main entry point that wires all modules together. Sequence: loadEnv → createLogger → createDatabase + runMigrations → InvestigationRepository → ClaudeClient → ToolRegistry (registers brave_web_search and google_fact_check_search with JSON.stringify wrappers) → InvestigationPipeline → createApp(repo) → createBot + createMessageHandler → app.listen → bot.start. Graceful shutdown on SIGINT/SIGTERM: stops bot, closes server, closes DB, exits. Tool handlers wrap `braveWebSearch` and `googleFactCheckSearch` return values with `JSON.stringify()` since `ToolHandler` expects `string` return type but the search functions return typed objects. Base URL for "View Full Analysis" links uses localhost in dev, production domain in prod.
- **Task 17.1:** `scripts/seed-demo.ts` runs 3 demo claims through the full pipeline with real API calls. Bootstraps same infrastructure as `index.ts` (minus bot and server). Claims run sequentially to avoid API rate limits. Sets dummy `TELEGRAM_BOT_TOKEN` when absent. Total cost: ~$1.68 for 3 claims (~$0.50-0.63 each). Duration: ~4-4.5 min per claim, ~12 min total. Confidence gate mismatch affects FALSE/MISLEADING claims (Judge says likely-false@97% → gate overrides to likely-true). TRUE claim correctly classified as partially-true@82%.
- **Task 17.2:** Dedicated `tests/unit/orchestrator/effort-escalation.test.ts` for the dynamic effort escalation feature. All implementation was already in place from Task 13.1 (pipeline.ts: disagreement detection, DA effort escalation to "max", deepReasoningActivated flag), Task 12.2 (telegram-formatter.ts: brain emoji indicator), and Task 15.2 (verdict.ejs: Deep Reasoning Mode badge in hero + footer). This task added 7 focused tests: 4 required (detect disagreement > 30, escalate to max, no escalation on agreement, set flag in verdict) + 3 bonus (no flag when agreed, exact boundary = 30 not triggered, boundary + 1 = 31 triggered).
- **Task 17.3:** `ClaimCache` class in `src/services/claim-cache.ts` implements a simple in-memory `Map<string, CacheEntry>` with TTL (default 1 hour). Keys are normalized: lowercase, trimmed, whitespace-collapsed. `set(claim, verdict, investigationId)` stores entries with `Date.now()` timestamp. `get(claim)` returns `CacheEntry | null`, deleting expired entries on access. Integrated into `InvestigationPipeline` constructor (optional `cache` param, defaults to `new ClaimCache()`). Cache check runs before any DB or API work — cache hit returns immediately with `cached: true` flag, `totalCostUsd: 0`, and the original `investigationId`. Only factual claim verdicts are cached (non-factual short-circuits don't store to cache). `InvestigateResult` type extended with optional `cached?: boolean` field.
- **Task T1.1:** Created `tests/fixtures/` module with factory functions for all 5 pipeline Zod types (`makeClassifierResult`, `makeSearchStrategy`, `makeAgentReport`, `makeChallengeReport`, `makeFinalVerdict`), plus `makeInvestigation` for DB row shape and `makeCannedBraveResults`/`makeCannedFactCheckResults` for mock search responses. `makeSearchStrategy` uses manual deep merge for nested objects instead of simple spread to avoid losing defaults. All factories validated against their Zod schemas in `tests/unit/fixtures/factories.test.ts` (13 tests).
- **Task T1.2:** Refactored 12 test files to use shared fixture factories from `tests/fixtures/`. Replaced local `makeVerdict()`, `sampleVerdict()`, `makeFakeVerdict()`, `makeClassifierResult()`, `makeSearchStrategy()`, `makeAgentReport()`, `makeChallengeReport()`, `makeFinalVerdict()` helpers with imports from the shared module. Files refactored: `pipeline.test.ts`, `effort-escalation.test.ts`, `devils-advocate-agent.test.ts`, `judge-agent.test.ts`, `confidence-gates.test.ts`, `telegram-formatter.test.ts`, `verdict-render.test.ts`, `verdict-sections.test.ts`, `verdict-details.test.ts`, `verdict.test.ts` (routes), `claim-cache.test.ts`, `message-handler.test.ts`. Used `makeFinalVerdict as makeVerdict` alias pattern where tests used a local `makeVerdict` name. All 336 tests pass with 0 regressions.
- **Task T6.1:** `scripts/quality-gate.sh` is a POSIX-compatible shell script that runs 4 sequential quality checks: (1) `npx tsc --noEmit`, (2) unit tests via `npx vitest run --exclude 'tests/integration/**'`, (3) coverage via `npx vitest run --coverage` with threshold check (line coverage >= 60%), (4) mocked integration tests (`api-e2e.test.ts`). Fail-fast: exits non-zero at the first failure. Coverage threshold parsed from `coverage/coverage-summary.json` using `node -e` (no `jq` dependency). Prints pass/fail summary with coverage percentage and duration. Added `npm run quality` script. Does NOT run live-API integration tests (opt-in only).
- **Task T6.2:** `scripts/quality-full.sh` runs the full quality gate PLUS all integration tests (including live-API E2E). Generates `quality-report.md` with: timestamp, git branch, commit SHA, TypeScript pass/fail, unit test counts (passed/failed/skipped), integration test counts (passed/failed), coverage percentages (line/branch/function), total API cost (parsed from `[E2E Pipeline] Total API cost:` log line), and total duration. Test counts parsed from vitest output by stripping ANSI codes with `sed` and matching with `grep -oE`. Added `npm run quality:full` script. `quality-report.md` added to `.gitignore`. Full run takes ~3-4 min and costs ~$0.55 (one E2E pipeline run).
- **UI Task 0.1:** Landing page skeleton. `GET /` route added to `createApp()` in `src/server/app.ts` (before health endpoint). Renders `landing.ejs` with `_landing-styles.ejs` partial. Same design system: dark theme, Satoshi/DM Mono/Instrument Serif fonts, noise texture, `--fc-*` CSS variables. Topbar with brand + GitHub link. Empty `<main>` element ready for section tasks. Landing page route does not require `InvestigationRepository` — works with `createApp()` (no args). Tests in `tests/unit/server/routes/landing.test.ts` (6 tests).
- **UI Task 0.2:** Extracted shared CSS design tokens into `src/server/views/_design-tokens.ejs` partial. Contains `:root` CSS variables (`--fc-bg`, `--fc-surface`, `--fc-surface-raised`, `--fc-border`, `--fc-text`, `--fc-text-muted`, `--fc-text-dim`, accent colors), base reset (`* { box-sizing }`), body styles, and noise texture SVG `body::before`. Both `_verdict-styles.ejs` and `_landing-styles.ejs` now include `_design-tokens.ejs` via `<%- include('_design-tokens') %>` instead of duplicating. Verdict styles retain the dynamic `--fc-verdict-color` EJS variable in a separate `:root` block. All 395 existing tests pass with 0 regressions.
- **UI Task 1.1:** Hero section with headline, subheadline, CTAs, and proof bar. Copy follows Power Writing Playbook: headline uses StoryBrand problem statement ("Lies travel fast. The truth needs backup."), subheadline presents the solution in active voice with "you" framing, CTAs point to value ("Try it on Telegram", "See a live verdict"). Proof bar acts as "But" killer (free, no sign-up, Opus 4.6). Radar ring animation behind hero (CSS `@keyframes` with 3 staggered rings + rotating sweep line). Gradient text accent on headline via `background-clip: text`. Responsive at 375px (stacked CTAs, smaller radar). `prefers-reduced-motion` disables all animations. Styles in `_landing-styles.ejs` (291 lines). 10 tests in `landing.test.ts`.
- **UI Task 1.2:** Animated demo card showing a fake fact-check in real-time — the "scroll-stopping" visual. Glass-morphism card (`backdrop-filter: blur(20px)`) with Telegram-style forwarded message bubble (blue left border, "Forwarded" label, Mars hoax claim). 5 pipeline steps appear sequentially via staggered `animation-delay` on a 12s loop: each step fades in, shows a spinner, then a green checkmark. Final verdict badge ("LIKELY FALSE — 94%") slides in after all steps complete. Pure CSS animation — no JS. 6 keyframe animations: `demo-msg-in`, `demo-step-lifecycle`, `demo-spinner-vis`, `demo-spin`, `demo-check-vis`, `demo-verdict-in`. `prefers-reduced-motion` shows all elements static. Styles partial grew to 532 lines (pure CSS). 14 tests in `landing.test.ts`.
- **UI Task 2.1:** "How It Works" pipeline visualization section showing all 5 pipeline steps (Classifier, Strategist, Investigators ×3, Devil's Advocate, Judge). Copy follows Power Writing Playbook: headline "What Happens When You Hit Forward" (curiosity gap), subtitle uses "you"/"your" framing. Desktop: horizontal flow with SVG arrow connectors between glass-morphism icon nodes. Mobile (≤768px): vertical timeline with gradient spine running down the left side. Model tier badges color-coded: Haiku (gray), Sonnet (blue), Opus (green glow). IntersectionObserver triggers staggered fade-in entrance animation (`.fc-pipeline-step--visible` class). `prefers-reduced-motion` disables transitions. Styles in `_landing-styles.ejs` (778 lines total, pure CSS). 17 tests in `landing.test.ts`. All 406 tests pass with 0 regressions.
- **UI Task 3.1:** Feature cards section with 3 glass-morphism cards showcasing ForwardCheck's differentiators. Copy drafted using Power Writing Playbook: titles use curiosity hooks ("See the Tricks", "Watch the Debate", "Four Scores, Not One"), body text uses "you"/"your" framing and visual language. Each card has a colored accent line at the top (red/blue/green) for visual variety without breaking cohesion. 3-col CSS grid on desktop, single column on mobile (≤768px). Glass-morphism cards with `backdrop-filter: blur(20px)`, hover lift + green glow. SVG icons (shield, eye, bar chart) in green-tinted containers. Styles partial grew to 895 lines (pure CSS). 20 tests in `landing.test.ts`. All 409 tests pass with 0 regressions.
- **UI Task 4.1:** Verdict preview "trust builder" section. Heading "Don't Take Our Word For It" (Trust Booster from Playbook). Glass-morphism card with inset darker background acts as a "window" into the real verdict page. Contains: Telegram-style forwarded claim quote (blue left border), red LIKELY FALSE badge with glowing dot, SVG confidence ring (donut chart showing 92%), 3-line summary in plain language, 4 confidence decomposition bars (evidence strength, source quality, claim complexity, counter-argument resilience) with color-coded gradient fills (red for low, amber for mid, green for high), and "Read the full analysis →" CTA to `/v/demo`. Static content (no JS needed) with realistic claim data. Subtle red radial glow behind the card for atmosphere. Responsive at 375px (tighter padding). Styles partial grew to 1085 lines (pure CSS). 24 tests in `landing.test.ts`. All 413 tests pass with 0 regressions.
- **UI Task 5.1:** Opus 4.6 showcase and tech stack section. Headline "Three Brains. One Mission." (curiosity gap from Playbook). 3-tier model pyramid: Opus 4.6 (top, narrowest, green glow), Sonnet (middle, blue), Haiku (bottom, widest, gray) — each tier is a glass-morphism card with colored left accent bar, model badge, role name, and one-line description. Pyramid visual narrows going up to convey increasing capability. 4 Opus 4.6 reasoning modes in a 2×2 grid: Strategic Planning, Adversarial Challenge, Tool-Augmented Verification, Confidence Decomposition. Tech stack pills: TypeScript, Grammy, Express, SQLite, Zod, Anthropic SDK. Extracted into `_opus-showcase.ejs` partial (102 lines). Also extracted verdict preview into `_verdict-preview.ejs` partial to keep `landing.ejs` under 400 lines (329 lines after extraction). Styles partial grew to 1492 lines (pure CSS). 28 tests in `landing.test.ts`. All 417 tests pass with 0 regressions.
- **UI Task 6.1:** Final CTA + footer — the StoryBrand "happy ending." Headline "Know Before You Share." (Instrument Serif, large). Subheadline uses "But" killers: "No sign-up. No cost." Primary CTA "Open in Telegram →" (`.fc-cta-primary.fc-final-cta-btn`, larger padding, `cta-pulse` keyframe animation on box-shadow for attention). Secondary CTA "View on GitHub →". Green radial glow behind CTA area (`rgba(34, 197, 94, 0.06)` elliptical gradient) for warm atmosphere. Footer: two lines of DM Mono dim text — "Built for Cerebral Valley × Anthropic Hackathon — Feb 2026" and "Powered by Claude Opus 4.6". Thin border-top separator. `cta-pulse` animation added to `prefers-reduced-motion` disable list. Mobile: tighter padding, stacked buttons. `landing.ejs` at 354 lines. Styles partial at 1605 lines. 32 tests in `landing.test.ts`. All 421 tests pass with 0 regressions.
